<Window x:Class="MessengerSvyaz.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MessengerSvyaz.ViewModels"
        xmlns:views="clr-namespace:MessengerSvyaz.Views"
        Title="Связь" 
        Height="700" Width="1100"
        MinHeight="500" MinWidth="800"
        Background="{StaticResource BgDarkBrush}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <views:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ChatViewModel}">
            <views:ChatView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProfileViewModel}">
            <views:ProfileView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:GroupsViewModel}">
            <views:GroupsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:GroupChatViewModel}">
            <views:GroupChatView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SupportViewModel}">
            <views:SupportView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:GroupInfoViewModel}">
            <views:GroupInfoView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <ContentControl Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>